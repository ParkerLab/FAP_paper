For each line, we performed light WGS in order to confirm line identity and prevent sample swaps.

First, census-seq tools were used as outlined here: https://github.com/ParkerLab/Census-Seq-Analysis

Roll call results were then used to confirm line identity and plot as a heatmap as outlined in heatmap.R
